services:
  app:
    image: atlas-traffic-calls
    build:
      context: ../..
      dockerfile: ./apps/traffic-calls/Dockerfile
    ports:
      - "${PORT:-3012}:${PORT:-3012}"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
      - PORT=${PORT:-3012}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=traffic-calls_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/traffic-calls_db
    depends_on:
      migrate:
        condition: service_completed_successfully
      seed:
        condition: service_completed_successfully
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3012/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  migrate:
    image: atlas-traffic-calls
    build:
      context: ../..
      dockerfile: ./apps/traffic-calls/Dockerfile
    command: node apps/traffic-calls/dist/db/migrate.js
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=traffic-calls_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/traffic-calls_db
    depends_on:
      postgres:
        condition: service_healthy
    restart: on-failure

  seed:
    image: atlas-traffic-calls
    build:
      context: ../..
      dockerfile: ./apps/traffic-calls/Dockerfile
    command: node apps/traffic-calls/dist/db/seed.js
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=traffic-calls_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/traffic-calls_db
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: on-failure
    profiles:
      - with-seed

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=traffic-calls_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
