services:
  app:
    image: atlas-tatical-urbanism
    build:
      context: ../..
      dockerfile: ./apps/tatical-urbanism/Dockerfile
    ports:
      - "${PORT:-3021}:${PORT:-3021}"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
      - PORT=${PORT:-3021}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=tatical-urbanism_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/tatical-urbanism_db
    depends_on:
      migrate:
        condition: service_completed_successfully
      seed:
        condition: service_completed_successfully
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3021/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  migrate:
    image: atlas-tatical-urbanism
    build:
      context: ../..
      dockerfile: ./apps/tatical-urbanism/Dockerfile
    command: node apps/tatical-urbanism/dist/db/migrate.js
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=tatical-urbanism_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/tatical-urbanism_db
    depends_on:
      postgres:
        condition: service_healthy
    restart: on-failure

  seed:
    image: atlas-tatical-urbanism
    build:
      context: ../..
      dockerfile: ./apps/tatical-urbanism/Dockerfile
    command: node apps/tatical-urbanism/dist/db/seed.js
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=tatical-urbanism_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/tatical-urbanism_db
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: on-failure
    profiles:
      - with-seed

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=tatical-urbanism_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
