services:
  app:
    image: atlas-cycle-infra-index
    build:
      context: ../..
      dockerfile: ./apps/cycle-infra-index/Dockerfile
    ports:
      - "${PORT:-6}:${PORT:-6}"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
      - PORT=${PORT:-6}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=cycle-infra-index_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/cycle-infra-index_db
    depends_on:
      migrate:
        condition: service_completed_successfully
      seed:
        condition: service_completed_successfully
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  migrate:
    image: atlas-cycle-infra-index
    build:
      context: ../..
      dockerfile: ./apps/cycle-infra-index/Dockerfile
    command: node apps/cycle-infra-index/dist/db/migrate.js
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=cycle-infra-index_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/cycle-infra-index_db
    depends_on:
      postgres:
        condition: service_healthy
    restart: on-failure

  seed:
    image: atlas-cycle-infra-index
    build:
      context: ../..
      dockerfile: ./apps/cycle-infra-index/Dockerfile
    command: node apps/cycle-infra-index/dist/db/seed.js
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=cycle-infra-index_db
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/cycle-infra-index_db
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: on-failure
    profiles:
      - with-seed

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=cycle-infra-index_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
